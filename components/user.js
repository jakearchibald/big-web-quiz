import { h, Component } from 'preact';

export class Login {
  render() {
    return (<a href="/login">Log in</a>);
  }
}

export class Logout {
  render() {
    return (<a href="/logout">Log out</a>);
  }
}